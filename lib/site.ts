// Single source of truth for the production origin — used by layout metadata, sitemap.ts,
// and robots.ts so they can't drift out of sync with each other.
export const SITE_URL = "https://fantasydraftboard.vercel.app";
